[project]
name = "restaurant-reservation-system"
version = "0.1.0"
description = "餐厅预定系统"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-sqlalchemy>=3.1.0",
    "flask-login>=0.6.3",
    "werkzeug>=3.0.0",
    "python-dateutil>=2.8.2",
]

[tool.uv]
